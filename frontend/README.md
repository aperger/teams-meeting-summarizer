# Frontend

This directory is reserved for the Angular frontend (Phase 8).

Planned API integrations:
- `POST /api/meetings/join`
- `POST /api/meetings/{connectionId}/leave`
- `GET /api/stream/summary/{connectionId}` (SSE)

